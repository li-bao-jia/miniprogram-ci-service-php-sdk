# Changelog

记录 `miniprogram-ci-service-php-sdk` 各版本（tag）改动。

---

## 维护规则

- 每次发布新 tag 时，新增对应版本章节（建议统一 `vX.Y.Z`）。
- 变更按 `Added / Changed / Fixed / Docs` 分类。
- 每个版本建议包含：发布日期、主要改动、兼容性说明（如有 Breaking Change）。


## [v1.0.0] - 2026-03-10

### Added
- 新增 PHP SDK 基础结构与核心调用能力。
- 新增客户端请求封装与配置对象（基础版本）。

